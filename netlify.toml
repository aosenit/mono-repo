[build]
  command = "nx build auth && nx build fincare-app"
  publish = "dist/apps/fincare-app"

[context.production.environment]
  NODE_ENV = "production"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
